---
# vars file for fail2ban

_fail2ban_default_jail:
  sshd: "{{ fail2ban_jail_sshd }}"
  dropbear: "{{ fail2ban_jail_dropbear }}"
  selinux-ssh: "{{ fail2ban_jail_selinux_ssh }}"
  apache-auth: "{{ fail2ban_jail_apache_auth }}"
  apache-badbots: "{{ fail2ban_jail_apache_badbots }}"
  apache-noscript: "{{ fail2ban_jail_apache_noscript }}"
  apache-overflows: "{{ fail2ban_jail_apache_overflows }}"
  apache-nohome: "{{ fail2ban_jail_apache_nohome }}"
  apache-botsearch: "{{ fail2ban_jail_apache_botsearch }}"
  apache-fakegooglebot: "{{ fail2ban_jail_apache_fakegooglebot }}"
  apache-modsecurity: "{{ fail2ban_jail_apache_modsecurity }}"
  apache-shellshock: "{{ fail2ban_jail_apache_shellshock }}"
  openhab-auth: "{{ fail2ban_jail_openhab_auth }}"
  nginx-http-auth: "{{ fail2ban_jail_nginx_http_auth }}"
  nginx-limit-req: "{{ fail2ban_jail_nginx_limit_req }}"
  nginx-botsearch: "{{ fail2ban_jail_nginx_botsearch }}"
  php-url-fopen: "{{ fail2ban_jail_php_url_fopen }}"
  suhosin: "{{ fail2ban_jail_suhosin }}"
  lighttpd-auth: "{{ fail2ban_jail_lighttpd_auth }}"
  roundcube-auth: "{{ fail2ban_jail_roundcube_auth }}"
  openwebmail: "{{ fail2ban_jail_openwebmail }}"
  horde: "{{ fail2ban_jail_horde }}"
  groupoffice: "{{ fail2ban_jail_groupoffice }}"
  sogo-auth: "{{ fail2ban_jail_sogo_auth }}"
  tine20: "{{ fail2ban_jail_tine20 }}"
  drupal-auth: "{{ fail2ban_jail_drupal_auth }}"
  guacamole: "{{ fail2ban_jail_guacamole }}"
  monit: "{{ fail2ban_jail_monit }}"
  webmin-auth: "{{ fail2ban_jail_webmin_auth }}"
  froxlor-auth: "{{ fail2ban_jail_froxlor_auth }}"
  squid: "{{ fail2ban_jail_squid }}"
  3proxy: "{{ fail2ban_jail_3proxy }}"
  proftpd: "{{ fail2ban_jail_proftpd }}"
  pure-ftpd: "{{ fail2ban_jail_pure_ftpd }}"
  gssftpd: "{{ fail2ban_jail_gssftpd }}"
  wuftpd: "{{ fail2ban_jail_wuftpd }}"
  vsftpd: "{{ fail2ban_jail_vsftpd }}"
  assp: "{{ fail2ban_jail_assp }}"
  courier-smtp: "{{ fail2ban_jail_courier_smtp }}"
  postfix: "{{ fail2ban_jail_postfix }}"
  postfix-rbl: "{{ fail2ban_jail_postfix_rbl }}"
  sendmail-auth: "{{ fail2ban_jail_sendmail_auth }}"
  sendmail-reject: "{{ fail2ban_jail_sendmail_reject }}"
  qmail-rbl: "{{ fail2ban_jail_qmail_rbl }}"
  dovecot: "{{ fail2ban_jail_dovecot }}"
  sieve: "{{ fail2ban_jail_sieve }}"
  solid-pop3d: "{{ fail2ban_jail_solid_pop3d }}"
  exim: "{{ fail2ban_jail_exim }}"
  exim-spam: "{{ fail2ban_jail_exim_spam }}"
  kerio: "{{ fail2ban_jail_kerio }}"
  courier-auth: "{{ fail2ban_jail_courier_auth }}"
  postfix-sasl: "{{ fail2ban_jail_postfix_sasl }}"
  perdition: "{{ fail2ban_jail_perdition }}"
  squirrelmail: "{{ fail2ban_jail_squirrelmail }}"
  cyrus-imap: "{{ fail2ban_jail_cyrus_imap }}"
  uwimap-auth: "{{ fail2ban_jail_uwimap_auth }}"
  named-refused: "{{ fail2ban_jail_named_refused }}"
  nsd: "{{ fail2ban_jail_nsd }}"
  asterisk: "{{ fail2ban_jail_asterisk }}"
  freeswitch: "{{ fail2ban_jail_freeswitch }}"
  znc-adminlog: "{{ fail2ban_jail_znc_adminlog }}"
  mysqld-auth: "{{ fail2ban_jail_mysqld_auth }}"
  mongodb-auth: "{{ fail2ban_jail_mongodb_auth }}"
  recidive: "{{ fail2ban_jail_recidive }}"
  pam-generic: "{{ fail2ban_jail_pam_generic }}"
  xinetd-fail: "{{ fail2ban_jail_xinetd_fail }}"
  stunnel: "{{ fail2ban_jail_stunnel }}"
  ejabberd-auth: "{{ fail2ban_jail_ejabberd_auth }}"
  counter-strike: "{{ fail2ban_jail_counter_strike }}"
  softethervpn: "{{ fail2ban_jail_softethervpn }}"
  gitlab: "{{ fail2ban_jail_gitlab }}"
  grafana: "{{ fail2ban_jail_grafana }}"
  bitwarden: "{{ fail2ban_jail_bitwarden }}"
  centreon: "{{ fail2ban_jail_centreon }}"
  nagios: "{{ fail2ban_jail_nagios }}"
  oracleims: "{{ fail2ban_jail_oracleims }}"
  directadmin: "{{ fail2ban_jail_directadmin }}"
  portsentry: "{{ fail2ban_jail_portsentry }}"
  pass2allow-ftp: "{{ fail2ban_jail_pass2allow_ftp }}"
  murmur: "{{ fail2ban_jail_murmur }}"
  screensharingd: "{{ fail2ban_jail_screensharingd }}"
  haproxy-http-auth: "{{ fail2ban_jail_haproxy_http_auth }}"
  slapd: "{{ fail2ban_jail_slapd }}"
  domino-smtp: "{{ fail2ban_jail_domino_smtp }}"
  phpmyadmin-syslog: "{{ fail2ban_jail_phpmyadmin_syslog }}"
  zoneminder: "{{ fail2ban_jail_zoneminder }}"
  traefik-auth: "{{ fail2ban_jail_traefik_auth }}"
